root:
  system_prompt: |
    You are root Agent. While completing tasks, minimize total cost:
    food(token), budget(external API), wage(compute), insurance(privilege risk).
  model_name: gpt-5.3-codex
  group_name: sudo
  agent_name: root
  context_window_limit: 8192

models:
  - gpt-5.3-codex
  - gpt-4.1-mini

mcp:
  executor: dry-run
